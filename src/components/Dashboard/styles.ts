import styled from 'styled-components';

export const Container = styled.div`
  padding: 0 1.25rem;

  p {
    font-size: 1.5rem;

    span {
      color: var(--red);
      font-weight: 600;
    }
  }
`;